<Window x:Class="AiPromptAssistant.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AiPromptAssistant"
        mc:Ignorable="d"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"

        AllowsTransparency="True"
        Background="Transparent"
        Title="SplashWindow" Height="565" Width="782" Opacity="0.0" Loaded="Window_Loaded">
    <Window.Resources>
        <!-- Fade In -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="0.8" Duration="0:0:1"/>
        </Storyboard>

        <!-- Fade Out -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0.8" To="0" Duration="0:0:1"/>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <Image Source="/spash1.png" />

    </Grid>
</Window>
